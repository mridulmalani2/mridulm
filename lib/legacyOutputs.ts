/**
 * PHASE 0 (rebuild/PHASE_0_HOTFIX.md): Fragility and Exit Reality Check are cut from the
 * v2 output set and their live versions have known defects (rebuild/DIFF_LEDGER.md
 * L-1: sign-flipped margin stress; L-3: flag count with no detail). One flag gates every
 * surface that would show them — UI tabs, Excel sheets, and cover-page blocks — so the
 * workbook can never contradict the screen. Hidden, not deleted, until engine2 replaces them.
 */
export const SHOW_LEGACY_OUTPUTS = false;
