# FINANCIAL_DEFINITIONS.md — RETIRED (redirect)

This registry described the OLD engine's conventions and was retired at the Phase F
cutover tail (2026-07-24). **The single source of financial truth is now
[`lib/engine2/SPEC.md`](lib/engine2/SPEC.md)** (versioned, changelog-governed, adjudicated
by `tests/goldens/`). The last pre-deletion copy of this file lives at tag
`pre-deletion-lib-engine`.

Do not add definitions here. A convention that needs stating is a SPEC amendment
(changelog entry + golden re-derivation where numbers move + independent sign-off).
